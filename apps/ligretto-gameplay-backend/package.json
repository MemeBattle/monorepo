{
  "name": "@memebattle/ligretto-gameplay-backend",
  "version": "1.0.9",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "init:partner": "init-partner",
    "test": "NODE_ENV=test jest .",
    "test:ci": "pnpm test --ci --json --coverage --testLocationInResults --outputFile=report.json",
    "test:update-snapshot": "pnpm test -u",
    "test:watch": "NODE_ENV=test jest --watch",
    "start:dev": "pnpm prestart && nodemon",
    "start": "pnpm prestart && ts-node src/index.ts",
    "prestart": "ts-node src/cli/check-env.ts",
    "build": "tsc --project tsconfig.build.json",
    "ts-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@jest/globals": "catalog:",
    "@types/lodash": "catalog:",
    "@types/node": "catalog:",
    "cross-env": "catalog:",
    "jest": "catalog:",
    "mock-socket": "catalog:",
    "nodemon": "catalog:",
    "ts-jest": "catalog:",
    "ts-node": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "@codexsoft/dotenv-flow": "catalog:",
    "@memebattle/cas-services": "workspace:^",
    "@memebattle/ligretto-shared": "workspace:^",
    "axios": "catalog:",
    "inversify": "catalog:",
    "lodash": "catalog:",
    "nanoid": "catalog:",
    "prom-client": "catalog:",
    "reflect-metadata": "catalog:",
    "socket.io": "catalog:"
  },
  "files": [
    "build"
  ]
}
