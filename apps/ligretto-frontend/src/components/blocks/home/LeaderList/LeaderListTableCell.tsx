import type { GridProps } from '@memebattle/ui'
import { Grid } from '@memebattle/ui'

export const LeaderListTableCell = (props: GridProps) => <Grid item alignItems="center" display="flex" {...props} />

LeaderListTableCell.displayName = 'LeaderListTableCell' //TODO это прототип?
