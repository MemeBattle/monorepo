import type { GridProps } from '@memebattle/ui'
import { Grid } from '@memebattle/ui'

export const GameResultTableCell = (props: GridProps) => <Grid item alignItems="center" display="flex" {...props} />

GameResultTableCell.displayName = 'GameResultTableCell'
