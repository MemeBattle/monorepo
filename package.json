  {
  "name": "@memebattle/memebattle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint:js": "eslint --ext .js,.vue,.jsx,.ts,.tsx .",
    "lint:js:fix": "yarn lint:js --fix",
    "lint:styles": "stylelint {apps,packages}/**/*.scss",
    "test": "lerna run --no-bail test --ignore-path .gitignore",
    "lerna-publish": "lerna publish from-package",
    "lerna-ci-publish": "yarn lerna publish --yes --no-git-reset from-package",
    "ligretto:start": "yarn lerna run --parallel --scope @memebattle/ligretto-shared --scope @memebattle/redux-utils --scope @memebattle/ligretto-backend --scope @memebattle/ligretto-recovery --scope @memebattle/ligretto-ui --scope @memebattle/auth-front start:dev",
    "common-packages:build": "yarn lerna run --scope @memebattle/ligretto-shared build && yarn lerna run --parallel --scope @memebattle/redux-utils --scope @memebattle/ligretto-ui build"
  },
  "engines": {
    "node": ">=10"
  },
  "workspaces": [
    "packages/**",
    "apps/*"
  ],
  "command": {
    "publish": {
      "npmClient": "npm"
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@types/jest": "^24.0.19",
    "@types/node": "^12.11.5",
    "@typescript-eslint/eslint-plugin": "^3.9.1",
    "@typescript-eslint/eslint-plugin-tslint": "^3.9.1",
    "@typescript-eslint/parser": "^3.9.1",
    "eslint": "^7.7.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.4",
    "eslint-plugin-react": "^7.20.6",
    "eslint-plugin-react-app": "^6.2.2",
    "hygen": "^5.0.3",
    "lerna": "^3.22.1",
    "prettier": "^2.0.5",
    "stylelint": "^13.6.1",
    "stylelint-config-prettier": "^8.0.2",
    "stylelint-prettier": "^1.1.2",
    "typescript": "^4.0.2"
  }
}
