{
  "name": "@memebattle/memebattle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint:js": "eslint --ext .js,.vue,.jsx,.ts,.tsx .",
    "lint:js:fix": "yarn lint:js --fix",
    "lint:styles": "stylelint {apps,packages}/**/*.scss",
    "test": "lerna run --no-bail test --ignore-path .gitignore",
    "lerna-publish": "lerna publish from-package",
    "lerna-ci-publish": "yarn lerna publish --yes --no-git-reset from-package",
    "ligretto:start": "env-cmd yarn lerna run --parallel --scope @memebattle/ligretto-shared --scope @memebattle/ligretto-backend --scope @memebattle/ligretto-recovery --scope @memebattle/ligretto-ui --scope @memebattle/auth-front start:dev",
    "common-packages:build": "yarn lerna run --scope @memebattle/ligretto-shared build && yarn lerna run --parallel --scope @memebattle/ligretto-ui build && yarn lerna run --parallel --scope @memebattle/gamehub-client build && yarn lerna run --scope @memebattle/auth-front build:module",
    "ligretto:front:build": "yarn lerna run --stream --scope @memebattle/ligretto-recovery build",
    "ligretto:front:start:dev": "env-cmd yarn lerna run --stream --scope @memebattle/ligretto-recovery start:dev",
    "ligretto:back:build": "yarn lerna run --stream --scope @memebattle/ligretto-backend build",
    "ligretto:core:build": "yarn lerna run --stream --scope @memebattle/ligretto-core build",
    "ligretto:core:migrate": "yarn lerna run --stream --scope @memebattle/ligretto-core migrate",
    "ligretto:core:start:dev": "yarn lerna run --stream --scope @memebattle/ligretto-core start:dev",
    "ligretto:ui:build-storybook": "yarn lerna run --stream --scope @memebattle/ligretto-ui build-storybook"
  },
  "engines": {
    "node": ">=10"
  },
  "workspaces": [
    "packages/**",
    "apps/*"
  ],
  "command": {
    "publish": {
      "npmClient": "npm"
    }
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@types/jest": "^24.0.19",
    "@types/node": "^14.14.20",
    "@typescript-eslint/eslint-plugin": "^4.26.1",
    "@typescript-eslint/parser": "^4.26.1",
    "eslint": "^7.28.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "eslint-plugin-react": "^7.24.0",
    "eslint-plugin-react-app": "^6.2.2",
    "hygen": "^5.0.3",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1",
    "stylelint": "^13.8.0",
    "stylelint-config-prettier": "^8.0.2",
    "stylelint-prettier": "^1.2.0",
    "typescript": "4.3.2"
  },
  "dependencies": {
    "env-cmd": "^10.1.0"
  }
}
